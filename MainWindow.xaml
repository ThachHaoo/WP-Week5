<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="App1.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1"
    xmlns:model="using:App1.Model"
    xmlns:converter="using:App1.View.Converter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="App1">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.Resources>
            <converter:PriceConverter x:Key="PriceConverter"/>
        </Grid.Resources>
        
        <ListView ItemsSource="{x:Bind ViewModel.Mobiles}"
                  VerticalAlignment="Stretch"
                  Grid.Column="0">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Padding" Value="12"/>
                    <Setter Property="Margin" Value="0,12,0,12" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="model:Mobile">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal" Margin="12" VerticalAlignment="Center">
                            <TextBlock>
                                    <Run Text="{x:Bind Name}" Foreground="Red"/>
                                    <Run Text=" - "/>
                                    <Run Text="{x:Bind Manufacturer}" Foreground="LightBlue"/>
                                    <Run  Text=" - "/>
                                    <Run  Text="{Binding Price, Converter={StaticResource PriceConverter}}" Foreground="Green"/>
                            </TextBlock>
                        </StackPanel>
                        <!--Chưa cần dùng Mode=TwoWay, UpdateSourceTrigger=PropertyChanged bởi vì ở đây không có dữ liệu vào-->
                        <Border Grid.Column="1" Background="Gray" Margin="0,0,12,0" BorderBrush="Coral" CornerRadius="25" BorderThickness="4">
                            <Image Source="{x:Bind Image}" Width="200" Height="200"/>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
